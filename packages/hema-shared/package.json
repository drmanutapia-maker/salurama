{
  "name": "@salurama/hema-shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared types, BSA calculators, CKD-EPI and CIE-10 search for the HEMA module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^2.0.0",
    "typescript": "^5.7.2",
    "vitest": "^2.0.0"
  },
  "dependencies": {
    "zod": "^4.4.3"
  }
}
