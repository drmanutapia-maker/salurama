# Hoist all packages so Next.js can resolve workspace dependencies
public-hoist-pattern[]=*
strict-ssl=false

# Peer deps — avoid install failures on new packages
auto-install-peers=true
strict-peer-dependencies=false
